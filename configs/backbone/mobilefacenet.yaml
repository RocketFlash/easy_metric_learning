type: mobilefacenet
family: mobilefacenet
# Pretrained weights are not bundled; set checkpoint_path for external weights.
pretrained: False
checkpoint_path:
freeze: False
num_features: 512
dropout: 0.0
expansion: 2

norm_mean: [0.5, 0.5, 0.5]
norm_std:  [0.5, 0.5, 0.5]
